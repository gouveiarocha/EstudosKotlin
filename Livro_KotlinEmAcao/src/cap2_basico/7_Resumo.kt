package cap2_basico

/**
 * A palavra reservada fun é usada para declarar uma função e as palavras var e val declaram variaveis mutaveis e
 * imutaveis
 *
 * Templates de strings ajudam a evitar concatenação de Strings complicadas
 *
 * Classes para objetos de valor são expressas de forma consisa
 *
 * if agora é uma expressão com valor de retorno
 *
 * When é como o switch do java, porém mais potente.
 *
 * Não precisamos fazer cast explicito de uma variavel.
 *
 * Os laços for, while e do-while são semelhantes as suas contrapartidas de Java, porém o for agora é mais conveniente
 * quando precisamos iterar por um mapa ou por uma coleçãocom um indice.
 *
 * A sintaxe concisa 1..5 cria um intervalo. Intervalos e progressoes permitem que Kotlin utilize uma sintaxe e um
 * conjunto de abstrações imofpr,es e, laços for, além de possibilitar o uso dos operadores in e !in, que verificam
 * se um valor pertence a um intervalo.
 *
 * Para excessoes, Kotlin não exige que voce declare as exceções que podem ser lançadas por uma função.
 *
 */